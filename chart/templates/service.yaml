apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiwix-server-mcp.fullname" . }}
  labels:
    {{- include "kiwix-server-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiwix-server-mcp.selectorLabels" . | nindent 4 }}
